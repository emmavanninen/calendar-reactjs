import React from "react";


//! call props
const Title = (props) => {
    return (
        //! no this in stateless component
        <h1 className='nav-title'>Venue X Booking
        </h1>
    );
};

export default Title;